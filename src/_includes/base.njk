<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{{ title or 'LaunchBook AI' }}</title>
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" />
  <link rel="stylesheet" href="/style.css" />
</head>

<body class="bg-gray-50 text-gray-900">
  <!-- Header -->
  <header class="bg-white shadow">
    <div class="max-w-7xl mx-auto px-4 py-4 flex justify-between items-center">
      <h1 class="text-xl font-bold text-blue-600">LaunchBook AI</h1>
      <nav class="space-x-4 hidden md:flex" id="nav-guest">
        <a href="#how-it-works" class="text-gray-700 hover:underline">How It Works</a>
        <a href="#" class="text-gray-700 hover:underline">Tools</a>
        <a href="#" class="text-gray-700 hover:underline">Pricing</a>
        <a href="/login" class="text-gray-700 hover:underline">Sign In</a>
       <a href="/signup" class="bg-blue-600 text-white px-4 py-2 rounded hover:bg-blue-700">Get Started Now</a>
      </nav>
      <div class="hidden md:flex items-center space-x-4" id="nav-user" style="display:none;">
        <span id="user-name" class="text-gray-700 font-medium">👤 Welcome, Ashish</span>
        <a href="/dashboard" class="text-gray-700 hover:underline">Dashboard</a>
        <button onclick="signOut()" class="text-red-600 hover:underline">Sign Out</button>
      </div>
    </div>
  </header>

  <!-- Main Content -->
    {% block content %}  {% endblock %}
  </main>

  <!-- Footer main -->
  <footer class="bg-white mt-16 border-t py-6 text-center text-sm text-gray-500">
    &copy; {{ "now" | date("yyyy") }} LaunchBook AI. Built with ❤️ by Leostarearn Hub.
  </footer>

  <!-- JS to toggle view later -->
  <script>
    // Simulate login (temporary)
    const userLoggedIn = false;
    if (userLoggedIn) {
      document.getElementById("nav-user").style.display = "flex";
      document.getElementById("nav-guest").style.display = "none";
      document.getElementById("user-name").innerText = "👤 Welcome, Ashish";
    }
    function signOut() {
      alert("Sign-out logic will be here.");
    }
  </script>
</body>
</html>
